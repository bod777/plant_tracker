[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "pip install -r server/requirements.txt",
  "npm run build"
]

[start]
cmd = "uvicorn app.main:app --host=0.0.0.0 --port=$PORT --app-dir server"
