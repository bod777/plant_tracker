{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m venv /app/.venv && /app/.venv/bin/pip install -r server/requirements.txt && npm run build"
  },
  "deploy": {
    "startCommand": "/app/.venv/bin/uvicorn app.main:app --host=0.0.0.0 --port=$PORT --app-dir server",
    "restartPolicyType": "ON_FAILURE"
  }
}
